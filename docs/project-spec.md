# Specification

The runtime specification lives in
[`SPEC.md`](https://github.com/sanskarpan/eddy-runtime/blob/main/SPEC.md) at
the repository root. Start with [Architecture](ARCHITECTURE.md) for the
event → waker → queue → poll cycle, then read the SPEC sections it cites.
